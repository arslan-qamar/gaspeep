APP_BASE_URL
CORS_ALLOWED_ORIGINS

DB_HOST
DB_PORT
DB_USER
DB_PASSWORD
DB_NAME

PORT
ENV

JWT_SECRET

GOOGLE_OAUTH_ID
GOOGLE_OAUTH_SECRET
GOOGLE_VISION_API_KEY
# Backend callback URL (what you register in Google Console). For local dev this is often https://api.gaspeep.com/api/auth/oauth/google/callback
GOOGLE_OAUTH_REDIRECT
# Frontend success URL to which backend will redirect with the app token
FRONTEND_OAUTH_SUCCESS_URL
 # Optional cookie config
 AUTH_COOKIE_DOMAIN AUTH_COOKIE_SECURE
APPLE_OAUTH_IDAPPLE_OAUTH_SECRET
AWS_S3_BUCKETAWS_S3_REGIONAWS_ACCESS_KEYAWS_SECRET_KEY
FCM_SERVER_KEY
STRIPE_SECRET_KEYSTRIPE_PUBLISHABLE_KEY
# Local TLS cert/key for development (point to certs generated in frontend/certs)
# These are used when running `go run` locally. When running via docker-compose,
# docker-compose.yml mounts certs into the container and sets TLS_CERT/TLS_KEY.
TLS_CERT
TLS_KEY

# SMTP configuration for sending emails (e.g. password resets)
SMTP_HOST
SMTP_PORT
SMTP_USER
SMTP_PASS
EMAIL_FROM
